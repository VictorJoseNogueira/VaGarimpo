# persona:

## tarefa: 

##  ESCOPO:
Varra recursivamente apenas os arquivos com extensão `.py` localizados na raiz (`main.py`) e no diretório `src/`.
Ignore estritamente: `venv/`, arquivos de configuração (`.env`, `.gitignore`, `.dockerignore`, `docker-compose.yml`, `requirements.txt`, `*.json`) e documentações (`*.md`) e o arquivo `get_job.py`.
